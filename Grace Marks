#include <iostream>
using namespace std;

int main()
{
    float marks[5];
    
    for (int i=0;i<5;i++)
    {
        cin>>marks[i];
    }
    
    for (int i=0;i<5;i++)
    {
        marks[i]=marks[i]+marks[i]*0.05;
        cout<<marks[i]<<" ";
    }
    
    return 0;
}
